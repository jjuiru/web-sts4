<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mohaji.mapper.BoardMapper">
	<select id="selectCount" resultType="Integer">
		select count(*) from member
	</select>


	<select id="selectBoardList"
		resultType="com.mohaji.dto.BoardDto">
		<![CDATA[
			SELECT
				ID,
				EMAIL,
				MENO,
				AGE,
				NAME,
				DATE_FORMAT(REGDATE, '%Y.%m.%d %H:%i:%s') AS REGDATE
			FROM
				MEMBER
			ORDER BY id DESC
		]]>
	</select>

	<insert id="insertBoard" parameterType="com.mohaji.dto.BoardDto">
			<![CDATA[
INSERT INTO MEMBER (
    EMAIL,
    MENO,
    AGE,
    NAME,
    REGDATE)
VALUES 
(
    #{email},
    #{meno},
    #{age},
    #{name},
    now()
)
		]]>
	</insert>
	
	<delete id="deleteBoard" parameterType="int">
	delete from member where id= #{id}
	</delete>
	
	<update id="updateBoard" parameterType="com.mohaji.dto.BoardDto" >
				<![CDATA[
UPDATE MEMBER SET 
EMAIL=	#{email},
NAME=	#{name}, 
AGE=    #{age},
MENO=    #{meno}
WHERE ID= #{id}
		]]>
	</update>

</mapper>